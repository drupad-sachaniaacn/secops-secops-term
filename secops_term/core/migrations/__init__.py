"""SQL migrations applied by :mod:`secops_term.core.db`.

Filename format: ``NNN_short_name.sql`` where ``NNN`` is the integer version
(e.g. ``001_initial.sql``). Concrete migrations land in Phase 1+ as feature
modules need them; the migration runner itself is fully tested in Phase 0
with synthetic migrations.
"""
